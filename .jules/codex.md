## 2025-02-23 - README.md Code Example Errors
**Observation:** The README.md contains a syntax error in the code example: `var span2 = new Span<byte(mem);` which lacks the closing `>`. Additionally, there is an undefined variable `i` referenced in `var b = span.ReadInt32(i);`. The documentation claims 100% coverage, "coverate" has a typo.
**Strategic Action:** Update the README.md to fix the syntax errors, correcting `Span<byte(mem)` to `Span<byte>(mem)`, correcting `ReadInt32(i)` to `ReadInt32()` or an appropriate offset, and fix the typo "coverate" to "coverage". Also clarify the architecture to explicitly distinguish implemented functionality from planned hypotheses (e.g. noting that SpanStream/MemoryStreamer are explicitly lower-level, avoiding high-level UI assumptions).